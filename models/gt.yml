version: 2

models:
  - name: yellow_taxi
    columns:
      - name: '"VendorId"'
        tests:
          - unique
          - not_null
          - accepted_values:
              config:
                severity: error
              values:
                - 1

                

      - name: '"payment_type"'
        tests:
          - not_null
          - accepted_values:
              values:
                - 1

                
  
 
